"""Pydantic request/response schemas for the v1 API.

These schemas are distinct from application DTOs — they are the HTTP wire format.
Phase 03 defines only health schemas; later phases add feed, story, auth schemas.
"""
