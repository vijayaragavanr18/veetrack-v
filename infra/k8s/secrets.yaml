# IMPORTANT: This file contains placeholder base64 values.
# Replace every "CHANGE_ME" with real base64-encoded secrets before applying.
#
# Encode a value:  echo -n 'your-secret-value' | base64
#
# Do NOT commit real secrets to version control.
# In production, use Sealed Secrets, External Secrets Operator, or Vault.
apiVersion: v1
kind: Secret
metadata:
  name: veetrack-secrets
  namespace: veetrack
  labels:
    app.kubernetes.io/part-of: veetrack
type: Opaque
data:
  # Long random string — min 32 chars.  openssl rand -base64 48
  jwt-secret: Q0hBTkdFX01F

  # Postgres superuser password
  postgres-password: Q0hBTkdFX01F

  # Anthropic API key (sk-ant-...)
  anthropic-api-key: Q0hBTkdFX01F

  # Sentry DSN for error tracking (https://...@sentry.io/...)
  sentry-dsn: Q0hBTkdFX01F

  # Optional external connector keys
  newsdata-api-key: Q0hBTkdFX01F
  twitterapi-io-key: Q0hBTkdFX01F
